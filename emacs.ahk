#IfWinActive emacs  ; 判断当前激活的窗口是否是Emacs，这是根据窗口标题实现的，如果你的标题不一样，请替换一下 

;LControl::Capslock ; 把Control替换为Capslock 

Capslock::LControl   ; 把Capslock替换为Control 
;LControl::LAlt
;Lalt::LControl

;RControl::RAlt
;RAlt::RControl
#IfWinActive